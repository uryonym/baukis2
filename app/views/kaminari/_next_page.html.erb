<span class="next">
  <%=
    link_to_unless(
      current_page.last?,
      t('views.pagination.next').html_safe,
      url,
    ) { |name| content_tag(:span, name, class: 'disabled') }
  %>
</span>
