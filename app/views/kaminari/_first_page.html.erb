<span class="first">
  <%=
    link_to_unless(
      current_page.first?,
      t('views.pagination.first').html_safe,
      url,
    ) { |name| content_tag(:span, name, class: 'disabled') }
  %>
</span>
